#include <stdio.h>

/**
 * main - entry point
 * Description - print all single numbers
 * Return: Always 0 (success)
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf(number);
	}
	printf("\n");
	return (0);
}
