#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer.
 * Return: Always o (success)
 */
int _abs(int)
{
	int num;

	if (num < 0)
	{
		return (num * -1);
	}
	else
	{
		return (num);
	}
}
