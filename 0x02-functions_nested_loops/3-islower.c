#include "main.h"
/**
 * _islower - checks for lowercase  character
 * @c: The character it checks.
 * Return: 1 if c is lowercase
 * Return: 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
